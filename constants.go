package main

const sock = "/tmp/gstatus.sock"
const gsock = "/tmp/gsock.sock"

const (
	datePos    = 0
	batteryPos = 1
	ramPos     = 2
	cpuPos     = 3
	hourPos    = 4
	bannerPos  = 5

	red    int = 1
	yellow int = 2
	green  int = 3
)
