package main

const sock = "/tmp/gstatus.sock"
const gsock = "/tmp/gsock.sock"

const (
	datePos    = 0
	batteryPos = 1
	hourPos    = 2
	cpuPos     = 3
	bannerPos  = 4

	red    int = 1
	yellow int = 2
	green  int = 3
)
